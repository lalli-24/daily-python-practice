def cont_Subsequence(nums):
    ans=1
    count=1

    for x in range(1,len(nums)):
        if nums[x]>nums[x-1]:
            count+=1
        else:
            ans=max(ans,count)
            count=1

    ans=max(ans,count)
    return ans

print(cont_Subsequence([1,3,5,4,7]))
