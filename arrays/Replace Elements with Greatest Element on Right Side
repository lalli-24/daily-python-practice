
def replaceElement(arr):
    maxi=-1

    for x in range(len(arr)-1,-1,-1):
        temp=arr[x]
        arr[x]=maxi
        maxi=max(temp,maxi)
    return arr

print(replaceElement([17,18,5,4,6,1]))
